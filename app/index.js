(function () {
    /**
     * @author Wael Jammal
     *
     * The main entry point for the app.
     */
    var Bootstrap = require('./common/globals/bootstrap');
    // Global dependencies bootstrap
    var bootstrap = new Bootstrap();
    bootstrap.start();
})();
